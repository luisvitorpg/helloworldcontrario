#ifndef HEADER_H
#define HEADER_H

int HelloWorld(char (*printFunc)(const char *, ...));

#endif // HEADER_H
// This header file defines the function HelloWorld, which takes 
// a function pointer as an argument.